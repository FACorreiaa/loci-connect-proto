// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: loci/entitlement/v1/entitlement.proto

package entitlementv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/FACorreiaa/loci-connect-proto/gen/go/loci/entitlement/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EntitlementServiceName is the fully-qualified name of the EntitlementService service.
	EntitlementServiceName = "loci.entitlement.v1.EntitlementService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EntitlementServiceGetEntitlementsProcedure is the fully-qualified name of the
	// EntitlementService's GetEntitlements RPC.
	EntitlementServiceGetEntitlementsProcedure = "/loci.entitlement.v1.EntitlementService/GetEntitlements"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	entitlementServiceServiceDescriptor               = v1.File_loci_entitlement_v1_entitlement_proto.Services().ByName("EntitlementService")
	entitlementServiceGetEntitlementsMethodDescriptor = entitlementServiceServiceDescriptor.Methods().ByName("GetEntitlements")
)

// EntitlementServiceClient is a client for the loci.entitlement.v1.EntitlementService service.
type EntitlementServiceClient interface {
	GetEntitlements(context.Context, *connect.Request[v1.GetEntitlementsRequest]) (*connect.Response[v1.Entitlements], error)
}

// NewEntitlementServiceClient constructs a client for the loci.entitlement.v1.EntitlementService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEntitlementServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EntitlementServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &entitlementServiceClient{
		getEntitlements: connect.NewClient[v1.GetEntitlementsRequest, v1.Entitlements](
			httpClient,
			baseURL+EntitlementServiceGetEntitlementsProcedure,
			connect.WithSchema(entitlementServiceGetEntitlementsMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// entitlementServiceClient implements EntitlementServiceClient.
type entitlementServiceClient struct {
	getEntitlements *connect.Client[v1.GetEntitlementsRequest, v1.Entitlements]
}

// GetEntitlements calls loci.entitlement.v1.EntitlementService.GetEntitlements.
func (c *entitlementServiceClient) GetEntitlements(ctx context.Context, req *connect.Request[v1.GetEntitlementsRequest]) (*connect.Response[v1.Entitlements], error) {
	return c.getEntitlements.CallUnary(ctx, req)
}

// EntitlementServiceHandler is an implementation of the loci.entitlement.v1.EntitlementService
// service.
type EntitlementServiceHandler interface {
	GetEntitlements(context.Context, *connect.Request[v1.GetEntitlementsRequest]) (*connect.Response[v1.Entitlements], error)
}

// NewEntitlementServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEntitlementServiceHandler(svc EntitlementServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	entitlementServiceGetEntitlementsHandler := connect.NewUnaryHandler(
		EntitlementServiceGetEntitlementsProcedure,
		svc.GetEntitlements,
		connect.WithSchema(entitlementServiceGetEntitlementsMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/loci.entitlement.v1.EntitlementService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EntitlementServiceGetEntitlementsProcedure:
			entitlementServiceGetEntitlementsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEntitlementServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEntitlementServiceHandler struct{}

func (UnimplementedEntitlementServiceHandler) GetEntitlements(context.Context, *connect.Request[v1.GetEntitlementsRequest]) (*connect.Response[v1.Entitlements], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("loci.entitlement.v1.EntitlementService.GetEntitlements is not implemented"))
}
