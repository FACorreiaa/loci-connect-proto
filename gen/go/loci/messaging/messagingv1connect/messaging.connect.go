// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: loci/messaging/messaging.proto

package messagingv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	messaging "github.com/FACorreiaa/loci-connect-proto/v5/gen/go/loci/messaging"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// MessagingServiceName is the fully-qualified name of the MessagingService service.
	MessagingServiceName = "loci.messaging.MessagingService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// MessagingServiceGetLinkProcedure is the fully-qualified name of the MessagingService's GetLink
	// RPC.
	MessagingServiceGetLinkProcedure = "/loci.messaging.MessagingService/GetLink"
	// MessagingServiceCreateLinkCodeProcedure is the fully-qualified name of the MessagingService's
	// CreateLinkCode RPC.
	MessagingServiceCreateLinkCodeProcedure = "/loci.messaging.MessagingService/CreateLinkCode"
	// MessagingServiceUnlinkProcedure is the fully-qualified name of the MessagingService's Unlink RPC.
	MessagingServiceUnlinkProcedure = "/loci.messaging.MessagingService/Unlink"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	messagingServiceServiceDescriptor              = messaging.File_loci_messaging_messaging_proto.Services().ByName("MessagingService")
	messagingServiceGetLinkMethodDescriptor        = messagingServiceServiceDescriptor.Methods().ByName("GetLink")
	messagingServiceCreateLinkCodeMethodDescriptor = messagingServiceServiceDescriptor.Methods().ByName("CreateLinkCode")
	messagingServiceUnlinkMethodDescriptor         = messagingServiceServiceDescriptor.Methods().ByName("Unlink")
)

// MessagingServiceClient is a client for the loci.messaging.MessagingService service.
type MessagingServiceClient interface {
	// GetLink reports whether a platform is linked to the caller's account.
	GetLink(context.Context, *connect.Request[messaging.GetLinkRequest]) (*connect.Response[messaging.GetLinkResponse], error)
	// CreateLinkCode issues a short-lived, single-use code to send to the bot.
	//
	// This is the only moment an unlinked chat can reach an account, so the code
	// is deliberately weak on its own: it is typed by hand into a chat window,
	// and what makes that safe is the expiry, the single use, and a rate limit
	// on redemption rather than its length.
	CreateLinkCode(context.Context, *connect.Request[messaging.CreateLinkCodeRequest]) (*connect.Response[messaging.CreateLinkCodeResponse], error)
	// Unlink disconnects the chat. The conversation history stays with the
	// account; only the route to it is removed.
	Unlink(context.Context, *connect.Request[messaging.UnlinkRequest]) (*connect.Response[messaging.UnlinkResponse], error)
}

// NewMessagingServiceClient constructs a client for the loci.messaging.MessagingService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewMessagingServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) MessagingServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &messagingServiceClient{
		getLink: connect.NewClient[messaging.GetLinkRequest, messaging.GetLinkResponse](
			httpClient,
			baseURL+MessagingServiceGetLinkProcedure,
			connect.WithSchema(messagingServiceGetLinkMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		createLinkCode: connect.NewClient[messaging.CreateLinkCodeRequest, messaging.CreateLinkCodeResponse](
			httpClient,
			baseURL+MessagingServiceCreateLinkCodeProcedure,
			connect.WithSchema(messagingServiceCreateLinkCodeMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		unlink: connect.NewClient[messaging.UnlinkRequest, messaging.UnlinkResponse](
			httpClient,
			baseURL+MessagingServiceUnlinkProcedure,
			connect.WithSchema(messagingServiceUnlinkMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// messagingServiceClient implements MessagingServiceClient.
type messagingServiceClient struct {
	getLink        *connect.Client[messaging.GetLinkRequest, messaging.GetLinkResponse]
	createLinkCode *connect.Client[messaging.CreateLinkCodeRequest, messaging.CreateLinkCodeResponse]
	unlink         *connect.Client[messaging.UnlinkRequest, messaging.UnlinkResponse]
}

// GetLink calls loci.messaging.MessagingService.GetLink.
func (c *messagingServiceClient) GetLink(ctx context.Context, req *connect.Request[messaging.GetLinkRequest]) (*connect.Response[messaging.GetLinkResponse], error) {
	return c.getLink.CallUnary(ctx, req)
}

// CreateLinkCode calls loci.messaging.MessagingService.CreateLinkCode.
func (c *messagingServiceClient) CreateLinkCode(ctx context.Context, req *connect.Request[messaging.CreateLinkCodeRequest]) (*connect.Response[messaging.CreateLinkCodeResponse], error) {
	return c.createLinkCode.CallUnary(ctx, req)
}

// Unlink calls loci.messaging.MessagingService.Unlink.
func (c *messagingServiceClient) Unlink(ctx context.Context, req *connect.Request[messaging.UnlinkRequest]) (*connect.Response[messaging.UnlinkResponse], error) {
	return c.unlink.CallUnary(ctx, req)
}

// MessagingServiceHandler is an implementation of the loci.messaging.MessagingService service.
type MessagingServiceHandler interface {
	// GetLink reports whether a platform is linked to the caller's account.
	GetLink(context.Context, *connect.Request[messaging.GetLinkRequest]) (*connect.Response[messaging.GetLinkResponse], error)
	// CreateLinkCode issues a short-lived, single-use code to send to the bot.
	//
	// This is the only moment an unlinked chat can reach an account, so the code
	// is deliberately weak on its own: it is typed by hand into a chat window,
	// and what makes that safe is the expiry, the single use, and a rate limit
	// on redemption rather than its length.
	CreateLinkCode(context.Context, *connect.Request[messaging.CreateLinkCodeRequest]) (*connect.Response[messaging.CreateLinkCodeResponse], error)
	// Unlink disconnects the chat. The conversation history stays with the
	// account; only the route to it is removed.
	Unlink(context.Context, *connect.Request[messaging.UnlinkRequest]) (*connect.Response[messaging.UnlinkResponse], error)
}

// NewMessagingServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewMessagingServiceHandler(svc MessagingServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	messagingServiceGetLinkHandler := connect.NewUnaryHandler(
		MessagingServiceGetLinkProcedure,
		svc.GetLink,
		connect.WithSchema(messagingServiceGetLinkMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	messagingServiceCreateLinkCodeHandler := connect.NewUnaryHandler(
		MessagingServiceCreateLinkCodeProcedure,
		svc.CreateLinkCode,
		connect.WithSchema(messagingServiceCreateLinkCodeMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	messagingServiceUnlinkHandler := connect.NewUnaryHandler(
		MessagingServiceUnlinkProcedure,
		svc.Unlink,
		connect.WithSchema(messagingServiceUnlinkMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/loci.messaging.MessagingService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case MessagingServiceGetLinkProcedure:
			messagingServiceGetLinkHandler.ServeHTTP(w, r)
		case MessagingServiceCreateLinkCodeProcedure:
			messagingServiceCreateLinkCodeHandler.ServeHTTP(w, r)
		case MessagingServiceUnlinkProcedure:
			messagingServiceUnlinkHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedMessagingServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedMessagingServiceHandler struct{}

func (UnimplementedMessagingServiceHandler) GetLink(context.Context, *connect.Request[messaging.GetLinkRequest]) (*connect.Response[messaging.GetLinkResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("loci.messaging.MessagingService.GetLink is not implemented"))
}

func (UnimplementedMessagingServiceHandler) CreateLinkCode(context.Context, *connect.Request[messaging.CreateLinkCodeRequest]) (*connect.Response[messaging.CreateLinkCodeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("loci.messaging.MessagingService.CreateLinkCode is not implemented"))
}

func (UnimplementedMessagingServiceHandler) Unlink(context.Context, *connect.Request[messaging.UnlinkRequest]) (*connect.Response[messaging.UnlinkResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("loci.messaging.MessagingService.Unlink is not implemented"))
}
