// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: loci/speech/speech.proto

package speechv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	speech "github.com/FACorreiaa/loci-connect-proto/v5/gen/go/loci/speech"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SpeechServiceName is the fully-qualified name of the SpeechService service.
	SpeechServiceName = "loci.speech.SpeechService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SpeechServiceTranscribeProcedure is the fully-qualified name of the SpeechService's Transcribe
	// RPC.
	SpeechServiceTranscribeProcedure = "/loci.speech.SpeechService/Transcribe"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	speechServiceServiceDescriptor          = speech.File_loci_speech_speech_proto.Services().ByName("SpeechService")
	speechServiceTranscribeMethodDescriptor = speechServiceServiceDescriptor.Methods().ByName("Transcribe")
)

// SpeechServiceClient is a client for the loci.speech.SpeechService service.
type SpeechServiceClient interface {
	// Transcribe returns what was said in a recording.
	Transcribe(context.Context, *connect.Request[speech.TranscribeRequest]) (*connect.Response[speech.TranscribeResponse], error)
}

// NewSpeechServiceClient constructs a client for the loci.speech.SpeechService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSpeechServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SpeechServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &speechServiceClient{
		transcribe: connect.NewClient[speech.TranscribeRequest, speech.TranscribeResponse](
			httpClient,
			baseURL+SpeechServiceTranscribeProcedure,
			connect.WithSchema(speechServiceTranscribeMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// speechServiceClient implements SpeechServiceClient.
type speechServiceClient struct {
	transcribe *connect.Client[speech.TranscribeRequest, speech.TranscribeResponse]
}

// Transcribe calls loci.speech.SpeechService.Transcribe.
func (c *speechServiceClient) Transcribe(ctx context.Context, req *connect.Request[speech.TranscribeRequest]) (*connect.Response[speech.TranscribeResponse], error) {
	return c.transcribe.CallUnary(ctx, req)
}

// SpeechServiceHandler is an implementation of the loci.speech.SpeechService service.
type SpeechServiceHandler interface {
	// Transcribe returns what was said in a recording.
	Transcribe(context.Context, *connect.Request[speech.TranscribeRequest]) (*connect.Response[speech.TranscribeResponse], error)
}

// NewSpeechServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSpeechServiceHandler(svc SpeechServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	speechServiceTranscribeHandler := connect.NewUnaryHandler(
		SpeechServiceTranscribeProcedure,
		svc.Transcribe,
		connect.WithSchema(speechServiceTranscribeMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/loci.speech.SpeechService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SpeechServiceTranscribeProcedure:
			speechServiceTranscribeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSpeechServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSpeechServiceHandler struct{}

func (UnimplementedSpeechServiceHandler) Transcribe(context.Context, *connect.Request[speech.TranscribeRequest]) (*connect.Response[speech.TranscribeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("loci.speech.SpeechService.Transcribe is not implemented"))
}
