// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: loci/memory/memory.proto

package memoryv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	memory "github.com/FACorreiaa/loci-connect-proto/v5/gen/go/loci/memory"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// MemoryServiceName is the fully-qualified name of the MemoryService service.
	MemoryServiceName = "loci.memory.MemoryService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// MemoryServiceGetMemoryProcedure is the fully-qualified name of the MemoryService's GetMemory RPC.
	MemoryServiceGetMemoryProcedure = "/loci.memory.MemoryService/GetMemory"
	// MemoryServiceForgetTraitProcedure is the fully-qualified name of the MemoryService's ForgetTrait
	// RPC.
	MemoryServiceForgetTraitProcedure = "/loci.memory.MemoryService/ForgetTrait"
	// MemoryServiceForgetEvidenceProcedure is the fully-qualified name of the MemoryService's
	// ForgetEvidence RPC.
	MemoryServiceForgetEvidenceProcedure = "/loci.memory.MemoryService/ForgetEvidence"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	memoryServiceServiceDescriptor              = memory.File_loci_memory_memory_proto.Services().ByName("MemoryService")
	memoryServiceGetMemoryMethodDescriptor      = memoryServiceServiceDescriptor.Methods().ByName("GetMemory")
	memoryServiceForgetTraitMethodDescriptor    = memoryServiceServiceDescriptor.Methods().ByName("ForgetTrait")
	memoryServiceForgetEvidenceMethodDescriptor = memoryServiceServiceDescriptor.Methods().ByName("ForgetEvidence")
)

// MemoryServiceClient is a client for the loci.memory.MemoryService service.
type MemoryServiceClient interface {
	// GetMemory returns the caller's learned profile.
	GetMemory(context.Context, *connect.Request[memory.GetMemoryRequest]) (*connect.Response[memory.GetMemoryResponse], error)
	// ForgetTrait removes a belief and the signals that produced it. Deleting the
	// trait alone would be undone by the next recompute, so this reaches the
	// evidence.
	ForgetTrait(context.Context, *connect.Request[memory.ForgetTraitRequest]) (*connect.Response[memory.ForgetTraitResponse], error)
	// ForgetEvidence removes a single recorded action, leaving an otherwise
	// accurate belief intact.
	ForgetEvidence(context.Context, *connect.Request[memory.ForgetEvidenceRequest]) (*connect.Response[memory.ForgetEvidenceResponse], error)
}

// NewMemoryServiceClient constructs a client for the loci.memory.MemoryService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewMemoryServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) MemoryServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &memoryServiceClient{
		getMemory: connect.NewClient[memory.GetMemoryRequest, memory.GetMemoryResponse](
			httpClient,
			baseURL+MemoryServiceGetMemoryProcedure,
			connect.WithSchema(memoryServiceGetMemoryMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		forgetTrait: connect.NewClient[memory.ForgetTraitRequest, memory.ForgetTraitResponse](
			httpClient,
			baseURL+MemoryServiceForgetTraitProcedure,
			connect.WithSchema(memoryServiceForgetTraitMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		forgetEvidence: connect.NewClient[memory.ForgetEvidenceRequest, memory.ForgetEvidenceResponse](
			httpClient,
			baseURL+MemoryServiceForgetEvidenceProcedure,
			connect.WithSchema(memoryServiceForgetEvidenceMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// memoryServiceClient implements MemoryServiceClient.
type memoryServiceClient struct {
	getMemory      *connect.Client[memory.GetMemoryRequest, memory.GetMemoryResponse]
	forgetTrait    *connect.Client[memory.ForgetTraitRequest, memory.ForgetTraitResponse]
	forgetEvidence *connect.Client[memory.ForgetEvidenceRequest, memory.ForgetEvidenceResponse]
}

// GetMemory calls loci.memory.MemoryService.GetMemory.
func (c *memoryServiceClient) GetMemory(ctx context.Context, req *connect.Request[memory.GetMemoryRequest]) (*connect.Response[memory.GetMemoryResponse], error) {
	return c.getMemory.CallUnary(ctx, req)
}

// ForgetTrait calls loci.memory.MemoryService.ForgetTrait.
func (c *memoryServiceClient) ForgetTrait(ctx context.Context, req *connect.Request[memory.ForgetTraitRequest]) (*connect.Response[memory.ForgetTraitResponse], error) {
	return c.forgetTrait.CallUnary(ctx, req)
}

// ForgetEvidence calls loci.memory.MemoryService.ForgetEvidence.
func (c *memoryServiceClient) ForgetEvidence(ctx context.Context, req *connect.Request[memory.ForgetEvidenceRequest]) (*connect.Response[memory.ForgetEvidenceResponse], error) {
	return c.forgetEvidence.CallUnary(ctx, req)
}

// MemoryServiceHandler is an implementation of the loci.memory.MemoryService service.
type MemoryServiceHandler interface {
	// GetMemory returns the caller's learned profile.
	GetMemory(context.Context, *connect.Request[memory.GetMemoryRequest]) (*connect.Response[memory.GetMemoryResponse], error)
	// ForgetTrait removes a belief and the signals that produced it. Deleting the
	// trait alone would be undone by the next recompute, so this reaches the
	// evidence.
	ForgetTrait(context.Context, *connect.Request[memory.ForgetTraitRequest]) (*connect.Response[memory.ForgetTraitResponse], error)
	// ForgetEvidence removes a single recorded action, leaving an otherwise
	// accurate belief intact.
	ForgetEvidence(context.Context, *connect.Request[memory.ForgetEvidenceRequest]) (*connect.Response[memory.ForgetEvidenceResponse], error)
}

// NewMemoryServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewMemoryServiceHandler(svc MemoryServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	memoryServiceGetMemoryHandler := connect.NewUnaryHandler(
		MemoryServiceGetMemoryProcedure,
		svc.GetMemory,
		connect.WithSchema(memoryServiceGetMemoryMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	memoryServiceForgetTraitHandler := connect.NewUnaryHandler(
		MemoryServiceForgetTraitProcedure,
		svc.ForgetTrait,
		connect.WithSchema(memoryServiceForgetTraitMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	memoryServiceForgetEvidenceHandler := connect.NewUnaryHandler(
		MemoryServiceForgetEvidenceProcedure,
		svc.ForgetEvidence,
		connect.WithSchema(memoryServiceForgetEvidenceMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/loci.memory.MemoryService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case MemoryServiceGetMemoryProcedure:
			memoryServiceGetMemoryHandler.ServeHTTP(w, r)
		case MemoryServiceForgetTraitProcedure:
			memoryServiceForgetTraitHandler.ServeHTTP(w, r)
		case MemoryServiceForgetEvidenceProcedure:
			memoryServiceForgetEvidenceHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedMemoryServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedMemoryServiceHandler struct{}

func (UnimplementedMemoryServiceHandler) GetMemory(context.Context, *connect.Request[memory.GetMemoryRequest]) (*connect.Response[memory.GetMemoryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("loci.memory.MemoryService.GetMemory is not implemented"))
}

func (UnimplementedMemoryServiceHandler) ForgetTrait(context.Context, *connect.Request[memory.ForgetTraitRequest]) (*connect.Response[memory.ForgetTraitResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("loci.memory.MemoryService.ForgetTrait is not implemented"))
}

func (UnimplementedMemoryServiceHandler) ForgetEvidence(context.Context, *connect.Request[memory.ForgetEvidenceRequest]) (*connect.Response[memory.ForgetEvidenceResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("loci.memory.MemoryService.ForgetEvidence is not implemented"))
}
