// @generated by protoc-gen-es v2.10.1
// @generated from file loci/speech/speech.proto (package loci.speech, syntax proto3)
/* eslint-disable */

import type { GenFile, GenMessage, GenService } from "@bufbuild/protobuf/codegenv2";
import type { Message } from "@bufbuild/protobuf";

/**
 * Describes the file loci/speech/speech.proto.
 */
export declare const file_loci_speech_speech: GenFile;

/**
 * @generated from message loci.speech.TranscribeRequest
 */
export declare type TranscribeRequest = Message<"loci.speech.TranscribeRequest"> & {
  /**
   * The recording. Ten megabytes is far more than a minute of speech in any
   * sane encoding, and far less than anything worth buffering from a caller.
   *
   * @generated from field: bytes audio = 1;
   */
  audio: Uint8Array;

  /**
   * What the bytes are, e.g. "audio/wav" or "audio/ogg". Required: the model
   * is told this rather than sniffing it, and guessing wrong reads as a
   * recording full of noise.
   *
   * @generated from field: string mime_type = 2;
   */
  mimeType: string;
};

/**
 * Describes the message loci.speech.TranscribeRequest.
 * Use `create(TranscribeRequestSchema)` to create a new message.
 */
export declare const TranscribeRequestSchema: GenMessage<TranscribeRequest>;

/**
 * @generated from message loci.speech.TranscribeResponse
 */
export declare type TranscribeResponse = Message<"loci.speech.TranscribeResponse"> & {
  /**
   * What was said. Empty when there was no speech in the recording, which is
   * an ordinary outcome — a pocket, or a minute of traffic — and not an error.
   *
   * @generated from field: string text = 1;
   */
  text: string;
};

/**
 * Describes the message loci.speech.TranscribeResponse.
 * Use `create(TranscribeResponseSchema)` to create a new message.
 */
export declare const TranscribeResponseSchema: GenMessage<TranscribeResponse>;

/**
 * SpeechService turns a recording into the words in it, so somebody can say
 * what they want instead of typing it.
 *
 * Transcription only, and deliberately: the transcript goes back to whoever
 * spoke it rather than straight into a question. Speech recognition mangles
 * place names, and "Alfama" heard as "alarm" produces a confident itinerary
 * for the wrong city. Handing the words back first lets that be corrected in
 * the second it takes, instead of being discovered a minute later.
 *
 * What happens with the transcript afterwards is ChatService's job. Asking a
 * question is still one thing, whether it was typed or spoken.
 *
 * @generated from service loci.speech.SpeechService
 */
export declare const SpeechService: GenService<{
  /**
   * Transcribe returns what was said in a recording.
   *
   * @generated from rpc loci.speech.SpeechService.Transcribe
   */
  transcribe: {
    methodKind: "unary";
    input: typeof TranscribeRequestSchema;
    output: typeof TranscribeResponseSchema;
  },
}>;

